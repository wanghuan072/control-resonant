import LocationsPage from "@/page/locations/LocationsPage";
import { createMetadata } from "@/seo/metadata";

export const metadata = createMetadata({ path: "/locations" });

export default LocationsPage;
