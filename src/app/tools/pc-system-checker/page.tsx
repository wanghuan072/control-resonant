import HardwareCheckerPage from "@/page/tools/HardwareCheckerPage";
import { createMetadata } from "@/seo/metadata";

export const metadata = createMetadata({ path: "/tools/pc-system-checker" });

export default HardwareCheckerPage;
