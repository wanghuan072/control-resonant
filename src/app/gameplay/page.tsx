import GameplayPage from "@/page/gameplay/GameplayPage";
import { createMetadata } from "@/seo/metadata";

export const metadata = createMetadata({ path: "/gameplay" });

export default GameplayPage;
