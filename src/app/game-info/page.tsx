import GameInfoPage from "@/page/game-info/GameInfoPage";
import { createMetadata } from "@/seo/metadata";
export const metadata = createMetadata({ path: "/game-info" });
export default GameInfoPage;
