import ReleaseDatePage from "@/page/game-info/ReleaseDatePage";
import { createMetadata } from "@/seo/metadata";

export const metadata = createMetadata({
  path: "/game-info/release-date",
  keywords: [
    "CONTROL Resonant release date",
    "CONTROL Resonant early access",
    "CONTROL Resonant physical release",
  ],
});

export default ReleaseDatePage;
